// Package codingsource owns the validator-local association between one
// active harness container source address and its opaque workspace/model
// routes. It never trusts forwarding headers and never exposes bindings,
// source addresses, or capability tokens through diagnostic serialization.
package codingsource
